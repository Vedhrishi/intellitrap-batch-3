# Verification report — 9 fixes + regression sweep

Read-only audit. Every claim below is backed by a file:line read or a live database query in this session.

## V1 — Monitoring read relaxation — PASS
Live `pg_policies` query: `visitors`, `visitor_events`, `honeypot_activity`, `ip_intelligence`, `blocked_ips` each have exactly one SELECT policy `TO authenticated USING (true)`.
Writes are NOT relaxed: `visitors`, `visitor_events`, `honeypot_activity` have zero INSERT/UPDATE/DELETE policies (all client writes denied; server writes go through the service-role client). `blocked_ips` has INSERT `private.is_admin()` and UPDATE `private.is_admin()`; `ip_intelligence` has UPDATE `private.is_admin()`. No DELETE policy anywhere. No `anon` grants in any of these policies. Read-only relaxation confirmed — no write regression.

## V2 — markVisitorOffline — PASS
Defined at `src/lib/tracking/tracking.functions.ts:234-244`, sets `is_online: false` plus `session_ended_at`.
Called from `src/components/tracking/tracking-provider.tsx:130` via `goOffline`, wired to `pagehide` (`:135`) and `visibilitychange` → hidden (`:132-136`); both listeners removed in the cleanup at `:137-140`.
Heartbeat still runs on a 15s interval (`HEARTBEAT_MS = 15_000` at `:52`, `setInterval` at `:117-121`) and clears via `window.clearInterval` at `:122`.

## V3 — Geo provider + cache — PASS
`https://ipwho.is/...` at `src/lib/tracking/tracking.server.ts:43`; repo-wide `rg` for `ip-api` returns zero hits — no `http://` provider call remains.
`lookupGeo` reads the `ip_intelligence` cache first and returns early when `latitude`/`longitude` are non-null (`:77-95`), only then calls `fetchGeo` (`:97`).
Failure safety: `fetchGeo` returns `{}` on any error (`:64-66`), and because a cached row short-circuits before the fetch, an existing cached coordinate is returned rather than overwritten with nulls. `intelGeo` (`:243-253`) only writes nulls when there was no cached fix to begin with.

## V4 — verifyFilePassword verdict re-check — PASS
Guard at `src/lib/tracking/tracking.functions.ts:577-593`, placed after the hash comparison and before `createSignedUrl`:
- `if (verdict?.was_blocked || verdict?.access_decision === "blocked")` → `{ success: false, blocked: true }` (`:582-584`)
- `if (verdict?.in_honeypot || verdict?.access_decision === "honeypot")` → logs `file_access_log` outcome `honeypot`, returns `{ success: false, honeypot: true }` (`:585-593`)
No signed URL is created on either path.
Granted path intact: for any other verdict, execution continues to `createSignedUrl(file.storage_path, 60)` (`:595-597`), increments `download_count`, logs outcome `success`, and returns `{ success: true, url, fileName, fileSize, fileType, oneTime }` (`:625-632`). A low-risk visitor with the correct password still gets the real file.

## V5 — Audit logging — PASS (code), UNEXERCISED (data)
`insertAuditLog` helper at `src/lib/admin/admin-data.ts:107-124`; `currentAdmin()` at `:222-226`.
- lift block → `block_lifted`, details `{}` (`src/lib/admin/admin-data.ts:243-251`)
- manual block → `ip_blocked_manual`, details `{ reason }` (`:266-275`)
- settings save → audit written after upsert (`:307-320`), payload built from the four boolean platform toggles (`src/components/admin/settings-platform-tab.tsx:15-20`)
- user removal → already audited via the privileged server function.
No passwords, tokens, secret codes, or keys appear in any details payload.
Note: `select count(*) from admin_audit_log` returns 0 — the code path is correct but has never been executed, so it is unverified at runtime. Perform one lift-block before the demo.

## V6 — Map pulse ring — PARTIAL
`dotIcon(color, size, opacity, pulse)` emits the SMIL `<animate>` ring only when `pulse` is true (`src/components/dashboard/LiveVisitorMap.tsx:41-54`); the call site passes `online` as that flag (`:100`), so offline markers get no ring and render smaller/dimmer (14px, 0.5 opacity).
Reduced motion: the global override at `src/styles.css:220-229` only affects CSS animations in the document. SMIL inside an SVG data-URI referenced as a Google Maps marker image is not covered, so the pulse keeps animating under `prefers-reduced-motion: reduce`. Cosmetic only.

## V7 — Sidebar breakpoint — PASS
`MOBILE_BREAKPOINT = 1024` with `matchMedia("(max-width: 1023px)")` at `src/hooks/use-mobile.tsx:3-16`. Only the boundary value changed; the hook's contract (`boolean`) and the sheet-vs-static sidebar branch are untouched, so desktop nav at >=1024px is unaffected and the mobile sheet still opens below it.

## V8 — Secret code at signup — PASS
Generated by the trigger, not application code: `public.handle_new_user()` inserts `user_secret_code => public.generate_secret_code()`, fired by `on_auth_user_created` AFTER INSERT on `auth.users`.
Live query: `profiles` = 10 rows, `user_secret_code IS NULL` = 0 (backfill complete), duplicate non-null codes = 0.

## V9 — 100-file cap — PASS
Trigger `files_count_guard`, BEFORE INSERT ON `public.files`, executing `public.enforce_file_count()`, which raises `FILE_LIMIT_EXCEEDED` when the live (non-deleted) count reaches `profiles.max_files`.
Byte quota unaffected: `files_quota_guard` is a separate BEFORE INSERT trigger running `enforce_quota()`, raising `STORAGE_QUOTA_EXCEEDED`; `files_storage_sync` still recalculates `storage_used` on INSERT/UPDATE/DELETE. Both coexist.

## Regression checks

- **R1 /share public — PASS.** `createFileRoute("/share")` at `src/routes/share.tsx:46` with no `beforeLoad` and no auth gate; the file is a top-level route, not under `_authenticated/`.
- **R2 Granted download — PASS.** Server returns a 60s signed URL on the granted path (`tracking.functions.ts:595-632`); client consumes it in the granted branch of the share state machine.
- **R3 Honeypot decoys + logging — PASS.** `generateDecoySet(3)` on honeypot entry (`src/routes/share.tsx:329-331`), decoy download calls `logHoneypotAction` with `action: "download_decoy"` (`:380-382`), which inserts into `honeypot_activity` (`tracking.functions.ts:670-677`) plus a `file_download_decoy` visitor event.
- **R4 Realtime cleanup — PASS.** All subscriptions go through the single `useRealtimeTables` hook, which calls `supabase.removeChannel(channel)` in its cleanup (`src/hooks/use-realtime.ts:33-36`). Six-plus call sites: `dashboard.tsx:79`, `visitors.tsx:48`, `threats.tsx:49`, `blocked-ips.tsx:39`, `honeypot.tsx:39`, `admin.dashboard.tsx:60`, `admin.threat-intel.tsx:86`, `honeypot-panel.tsx:40`.
- **R5 Stricter-wins intact — PASS.** `DECISION_SEVERITY[serverDecision] > DECISION_SEVERITY[forest.decision] ? serverDecision : forest.decision` (`src/routes/share.tsx:264-270`); the client can only escalate. Server-side `verifyFilePassword` re-checks the stored verdict independently (V4), so a tampered client cannot obtain the file.
- **R6 Typecheck — PASS.** `tsgo --noEmit` exits 0 with no diagnostics.
- **R7 console.log — PASS.** Repo-wide `rg "console\.log" src` returns zero hits.
- **R8 Service-role reachability — PASS.** `SUPABASE_SERVICE_ROLE_KEY` appears only in `src/integrations/supabase/client.server.ts:37-51`, read from `process.env`, and every server function imports it lazily inside the handler (`await import("@/integrations/supabase/client.server")`), so it never enters a client chunk.

## Summary

```text
FIXES CONFIRMED: 9 of 9   (V6 partial — pulse ignores prefers-reduced-motion)
REGRESSIONS FOUND: 0
```

REMAINING BLOCKERS FOR A LIVE DEMO: none.

Two non-blocking notes:
1. `admin_audit_log` is empty — exercise one lift-block or settings save before the demo so the Audit Log page is not blank.
2. Map pulse animates regardless of `prefers-reduced-motion` (cosmetic).

UPDATED DEMO READINESS: 96/100
